# QA Test Plan — Memory Bank

Generated by `$init-project` from actual source/test surfaces.

## Scope modes

- **Feature scope (`--scope feature`)**: denominator is `.codex/loopy-era/qa-scenarios.json` and the changed/source scope recorded in each scenario.
- **Full regression (`--scope full-regression`)**: denominator is this full CRITICAL/HIGH/MEDIUM corpus.

## CRITICAL

| ID | Area | Coverage | Evidence command |
| --- | --- | --- | --- |
| TC-CRIT-001 | Build/package | TypeScript compile and MCP bundle succeed | `npm run build` |
| TC-CRIT-002 | Type safety | Strict TS source check succeeds | `npx tsc --noEmit` |
| TC-CRIT-003 | Core regression | All Vitest tests pass | `npm test` |
| TC-CRIT-004 | Hard process contract | Init/team/QA handoff is typed hard and validates | `python3 .codex/scripts/validate-hard-process-contract.py --project-root . --require-project-contract --json` |
| TC-CRIT-005 | MCP tool contract | `search`, `read`, facts/ontology/avatar/graph tools remain registered and validated | `npm test -- test/mcp-facts.test.ts` or full `npm test` |
| TC-CRIT-006 | DB migration/vector integrity | Schema migrations are idempotent and vec tables update with DELETE+INSERT | `npm test -- test/db.test.ts test/fact-db.test.ts test/knowledge-graph.test.ts` or full `npm test` |
| TC-CRIT-007 | Sync/index/search flow | JSONL sync/index/search works with isolated temp DB/archive | `npx vitest run test/integration.test.ts test/sync.test.ts` |
| TC-CRIT-008 | UI hard interaction gate | Dashboard interactions, API responses, persistent state assertions, clean console, and visual evidence are present | `python3 .codex/scripts/validate_hard_qa.py --scope feature --json` (currently expected FAIL with blockers) |

## HIGH

| ID | Area | Coverage | Evidence command |
| --- | --- | --- | --- |
| TC-HIGH-001 | Parser robustness | Short/medium/long JSONL, malformed-line tolerance | `npx vitest run test/parser.test.ts` |
| TC-HIGH-002 | Fact lifecycle | Extraction, persistence, consolidation, revisions | `npx vitest run test/fact-extractor.test.ts test/fact-integration.test.ts test/consolidator.test.ts` |
| TC-HIGH-003 | Search behavior | Text/vector/both/multi-concept formatting | `npx vitest run test/integration.test.ts test/multi-concept.test.ts test/search-format.test.ts` |
| TC-HIGH-004 | Path/env overrides | Config dir, DB path, archive path, excluded projects | `npx vitest run test/paths.test.ts test/verify.test.ts` |
| TC-HIGH-005 | Ontology/avatar | Classification/linking/grounded answers stay stable | `npx vitest run test/ontology-classifier.test.ts test/avatar-responder.test.ts` |
| TC-HIGH-006 | Web UI API | `/api/stats`, `/api/projects`, `/api/search`, `/api/exchange`, `/api/user-prompts`, `/api/project-detail`, `/api/graph-data` return asserted JSON | blocked until hard UI harness can launch a seeded DB/browser session |

## MEDIUM

| ID | Area | Coverage | Evidence command |
| --- | --- | --- | --- |
| TC-MED-001 | Stats/show formatting | CLI readable output remains stable | `npx vitest run test/stats.test.ts test/show.test.ts` |
| TC-MED-002 | Repeat/intent/session context | Context injection helpers remain accurate | `npx vitest run test/repeat-detector.test.ts test/intent-predictor.test.ts test/session-continuity.test.ts` |
| TC-MED-003 | Sync export/import | Cross-device sync artifacts preserve facts/ontology | `npx vitest run test/sync-export-import.test.ts` |
| TC-MED-004 | API config | Custom API endpoint/token/model behavior | `npx vitest run test/api-config.test.ts test/llm.test.ts` |

## Current hard UI blocker policy

The repo has a UI (`ui/server.cjs`) but no browser E2E dependency. The generated hard validator must fail until live browser interaction evidence exists or unsupported flows are explicitly handled with hard source/product evidence. Build/server/page-load checks alone are not completion evidence.
